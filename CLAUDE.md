# CLAUDE.md

All repository conventions, commands and gotchas live in
[AGENTS.md](./AGENTS.md). Read that file before making any changes.

It is kept tool-neutral because the team uses several different AI agents. Do not
duplicate its contents here — update `AGENTS.md` instead so every agent stays in
sync.
